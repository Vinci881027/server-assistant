# System status overview cache TTL (milliseconds)
app.system.status-overview-cache-ttl-ms=2000

# Async thread pool for parallel status collection
app.system.status.async.max-parallel=8
app.system.status.async.queue-capacity=64
app.system.status.async.keep-alive-seconds=30
